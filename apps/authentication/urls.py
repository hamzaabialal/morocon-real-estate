"""Authentication URL stub.

The real auth endpoints currently live in apps.agencies.urls
(/api/v1/auth/login/, /register/, /logout/). This stub exists so config.urls
can import an `urlpatterns` symbol without breaking.
"""
urlpatterns = []

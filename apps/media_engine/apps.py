from django.apps import AppConfig


class MediaEngineConfig(AppConfig):
    """Configuration for the media engine app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.media_engine"
